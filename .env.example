# Server Configuration
PORT=3001

# Email Configuration (Production)
# Leave empty to use Ethereal (development test email service)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="Ajman University Asset Management <assets@ajman.ac.ae>"

# Admin Email (receives copies of signed PDFs and dispute notifications)
ADMIN_EMAIL=store@ajman.ac.ae

# Base URL (used in emails for signing links)
BASE_URL=http://localhost:3000

# Reminder Service Configuration
# Cron schedule for automated reminders (default: daily at 9 AM)
# Format: minute hour day month weekday
# Examples:
#   0 9 * * * = Daily at 9 AM
#   */5 * * * * = Every 5 minutes (for testing)
REMINDER_CRON_SCHEDULE=0 9 * * *

# Timezone for reminder service (default: Asia/Dubai for Ajman University)
TZ=Asia/Dubai

# Environment (development/production)
NODE_ENV=development

# ===========================================
# OBSERVABILITY CONFIGURATION
# ===========================================

# Sentry Error Tracking (optional)
# Get your DSN from https://sentry.io
SENTRY_DSN=

# Logging Level (trace/debug/info/warn/error/fatal)
# Default: debug in development, info in production
LOG_LEVEL=info

# ===========================================
# SECURITY CONFIGURATION
# ===========================================

# Allowed Origins for CSRF protection (comma-separated)
# Default: http://localhost:3000,http://localhost:3001
# In production, set to your actual domain(s)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# ===========================================
# SMTP EXAMPLES
# ===========================================

# For production, use your organization's SMTP server:
# Example for Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Example for Office 365:
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@yourdomain.com
# SMTP_PASS=your-password
